{
	"compilerOptions": {
		// Base options:
		"skipLibCheck": true,
		"target": "es2024",
		"verbatimModuleSyntax": true,
		"allowJs": false,
		"moduleDetection": "force",
		// Strictness
		"strict": true,
		"noUncheckedIndexedAccess": true,
		// Type checking, not transpiling
		"module": "ESNext",
		"moduleResolution": "bundler",
		"lib": ["es2024", "DOM"],
		"declaration": true,
		"rootDir": "src",
		"rootDirs": ["src/lib", "src/cli"],
		"outDir": "dist",
		"types": ["node"],
		"paths": {
			// So we can import like an external in the CLI
			"@projectwallace/css-code-coverage": ["./src/lib/index.ts"]
		}
	},
	"include": ["src/lib/index.ts", "src/cli/cli.ts", "src/lib/*.d.ts"],
	"exclude": ["node_modules"]
}
